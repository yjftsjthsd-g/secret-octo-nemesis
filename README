My first attempt to write a virtual machine.
Currently loads with a program preloaded in memory or loads memory image file spec'd at command line.

The VM currently uses a 100-item main memory (for both code and data), and 10 general purpose registers.
